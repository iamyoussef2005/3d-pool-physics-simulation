export class GameController {
  constructor(sceneManager) {
    this.sceneManager = sceneManager;
    // Bind future controls, mouse events, keyboard controls, or aiming/shooting listeners here
  }
}
